var height=prompt("Enter your height");
power=Math.pow(height,2);
alert(power);

var num = prompt("Enter decimal number");
var rnd = Math.round(num);
var flr = Math.floor(num);
var ceil = Math.ceil(num);

alert ("Your num=" + num + ", round=" + rnd + ", floor="+flr + ", ceil="+ceil );
